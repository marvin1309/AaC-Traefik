---
# AUTOGENERATED FILE - DO NOT EDIT
# Source: traefik-dev-test SSoT (service.yml)
apiVersion: v1
kind: Service
metadata:
  name: traefik-dev-test
  namespace: networking
  labels:
    app: traefik-dev-test
spec:
  type: LoadBalancer
  selector:
    app: traefik-dev-test
  ports:
  - name: web
    port: 80
    targetPort: 80
    protocol: TCP
  - name: websecure
    port: 443
    targetPort: 443
    protocol: TCP
  - name: dashboard
    port: 8080
    targetPort: 8080
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik-dev-test-deployment
  namespace: networking
  labels:
    app: traefik-dev-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: traefik-dev-test
  template:
    metadata:
      labels:
        app: traefik-dev-test
    spec:
      containers:
      - name: traefik-dev-test
        image: "traefik:v3.0"
        ports:
        - containerPort: 80
          name: web
        - containerPort: 443
          name: websecure
        - containerPort: 8080
          name: dashboard
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"